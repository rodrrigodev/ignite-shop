import { createStitches } from '@stitches/react'

export const {config, css, keyframes, styled, globalCss, getCssText, createTheme, theme} = createStitches({
    theme: {
        colors: {
            rocketseat: '#8257e6'
        }
    }
})